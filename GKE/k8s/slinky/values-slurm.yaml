# Slurm chart overrides for GPU + Pyxis
# Assumes A100 nodes are labeled with: cloud.google.com/gke-accelerator=nvidia-tesla-a100

configFiles:
  gres.conf: |
    AutoDetect=nvidia
  plugstack.conf: |
    include /usr/share/pyxis/*

controller:
  extraConfMap:
    GresTypes: "gpu"

loginsets:
  pyxis:
    enabled: true
    replicas: 1
    login:
      image:
        repository: ghcr.io/slinkyproject/login-pyxis
        tag: 25.11-ubuntu24.04
      securityContext:
        privileged: true
      volumeMounts:
        - name: enroot-credentials
          mountPath: /root/.config/enroot
          readOnly: true
    podSpec:
      securityContext:
        fsGroup: 401
      volumes:
        - name: enroot-credentials
          secret:
            secretName: enroot-credentials
            optional: true
    sssdConf: |
      [sssd]
      config_file_version = 2
      services = nss,pam
      domains = LOCAL

      [nss]
      filter_groups = root,slurm
      filter_users = root,slurm

      [pam]

      [domain/LOCAL]
      id_provider = files
      auth_provider = files

nodesets:
  slinky:
    enabled: false
  gpu:
    enabled: true
    replicas: 2
    extraConfMap:
      Gres:
        - "gpu:1"
    slurmd:
      image:
        repository: ghcr.io/slinkyproject/slurmd-pyxis
        tag: 25.11-ubuntu24.04
      volumeMounts:
        - name: enroot-credentials
          mountPath: /root/.config/enroot
          readOnly: true
      resources:
        requests:
          cpu: "4"
          memory: 32Gi
          nvidia.com/gpu: 1
        limits:
          cpu: "8"
          memory: 64Gi
          nvidia.com/gpu: 1
    logfile:
      image:
        repository: docker.io/library/alpine
        tag: latest
      resources: {}
    useResourceLimits: true
    updateStrategy:
      type: RollingUpdate
      rollingUpdate:
        maxUnavailable: 25%
    taintKubeNodes: false
    podSpec:
      volumes:
        - name: enroot-credentials
          secret:
            secretName: enroot-credentials
            optional: true
      nodeSelector:
        cloud.google.com/gke-accelerator: nvidia-tesla-a100
      tolerations:
        - key: "nvidia.com/gpu"
          operator: "Exists"
          effect: "NoSchedule"
    partition:
      enabled: true
      configMap:
        State: UP
        Default: "YES"
        MaxTime: UNLIMITED
